using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using XR_Gestures;
public class InputManager : MonoBehaviour
{
    [SerializeField] GestureOutputManager outputManager;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

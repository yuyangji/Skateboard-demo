using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace XR_Gestures
{
   /* public class Rotation : FunctionObject
    {



        [SerializeField] float xThreshold;
        [SerializeField] float zThreshold;

        [SerializeField] float angleThreshold;
        [SerializeField] TrackerLabel tracker;

        State CheckAngle(float _angle) => _angle > angleThreshold ? State.ForceStart : State.Performing;


        bool Validate(Vector3 _deltaPos)
        => (Mathf.Abs(_deltaPos.x) > xThreshold) && (Mathf.Abs(_deltaPos.z) > zThreshold);



        private void OnValidate()
        {
            if (RotateOnToe && RotateOnHeel) RotateOnHeel = false;
        }

        protected override bool Function()
        {
            throw new System.NotImplementedException();
        }

        public override void Initialise(FunctionArgs _args)
        {
            throw new System.NotImplementedException();
        }
    }*/

}
